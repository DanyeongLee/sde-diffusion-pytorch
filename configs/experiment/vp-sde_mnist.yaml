# @package _global_

defaults:
  - override /train: train-default
  - override /model: unet-mnist
  - override /sde: vp-sde
  - override /optimizer: adamw
  - override /data: mnist
  - override /logger: wandb


sde:
  train_eps: 1e-5
  sample_eps: 1e-3

data:
  batch_size: 128

name: 'vp-sde_mnist'